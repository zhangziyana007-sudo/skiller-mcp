# Changelog

## v2.4.0 — 本地仓库统一架构 + 性能优化 + UI 重构 (2026-04-10)

### 架构升级：本地仓库统一视图
- **统一数据源** — 社区下载的技能存储到 `data/repository/`，项目配置数据源统一为"本地仓库"
- **本地仓库涵盖所有来源** — 全局 Skill (`~/.cursor/skills/`)、Cursor 内置 (`skills-cursor/`)、Superpowers 插件、社区下载 (`data/repository/`) 全部汇聚到一个视图
- **社区安装简化** — 从多种入口精简为两个按钮：「下载到本地仓库」/「安装为全局 Skill」

### UI 重构
- **技能卡片增强** — 项目技能卡片支持点击查看详情（描述 / Token 预估 / 内容预览）
- **社区上传快捷入口** — 本地仓库卡片新增 📤 上传到社区按钮
- **文字按钮替代图标** — 所有图标按钮改为文字按钮，提升可读性和可发现性

### 性能优化三件套
- **mtime 指纹缓存** — 扫描时比对目录 mtime，无变化时跳过索引重建，启动 0ms
- **延迟加载** — 项目规则按需扫描，不拖慢首屏渲染
- **HTML 拆分** — 381KB 单文件拆分为 `index.html`(5KB) + `style.css`(103KB) + `app.js`(272KB)，浏览器并行加载 + 缓存

### 技术统计
- 修改 8 文件
- +8,364 行 / -8,027 行

---

## v2.3.1 — 社区模块安全加固 + Bug 全量修复 (2026-04-10)

### 安全修复（高优先级）
- **#6** deleteSkill 改用源级 Token，不再泄露全局 Token 到非目标仓库
- **#12-14** 新增 `/api/community/fetch-content` 代理端点，私有源内容请求服务端携带 Token，前端不再暴露凭据
- **#15** Token 掩码值 (`••••xxxx`) 写回防护，编辑配置时不会把掩码覆盖真实 Token
- **#16** 敏感参数（Token、大段文本）从 URL query 迁移至 POST body，防止日志泄露
- **#28** `escapeHtml` 补充 `"` 和 `'` 引号转义，堵住潜在 XSS 向量

### 数据健壮性
- **#7** listSkillDirs 检测 GitHub Git Data API 的 `truncated` 响应，超限时自动回退 Contents API
- **#8** 多源技能列表通过 `seen` Set 去重，避免跨源重复显示
- **#9** 多源加载失败时返回 `_errors` 数组，前端可精准提示用户哪个源出了问题
- **#10** submitSkillViaIssue: 仓库无 `skill-submission` label 导致 422 时，自动去掉 label 重试
- **#24** rename-skill 增加 `rollbackCreated()` 回滚机制，创建新文件中途失败时自动清理已创建的文件

### 输入校验
- **#22** previewSkill URL 解析改用 `URL` 对象，替代脆弱的 `split('/')` 硬编码拼接
- **#26** uploadSkill 内容限制 500KB + 技能名正则校验（字母/数字/下划线/短横线）；description 限制 10KB
- **#25** 可写源浏览页显示下架按钮 — 检测 `writable` 标记而非仅匹配 `primary` sourceId

### 代码清理
- **#29** 删除未被引用的 `plaza.ts` 死代码模块（277 行）

### 技术统计
- 修改 3 文件，删除 1 文件
- +247 行 / -368 行（净减 121 行）

## v2.3.0 — 项目驱动工作区 + UX 全面优化 (2026-04-09)

### 架构重构：项目驱动视图
- **项目网格首页** — 以项目卡片网格替代传统技能列表，一目了然查看所有管理的项目
- **项目详情页** — 进入项目后分「已安装」和「可添加」两个区域，直观管理技能
- **全局技能视图** — 保留独立 tab 查看所有全局技能

### 项目管理
- **手动管理** — 持久化项目列表，支持手动添加/删除项目
- **Cursor 历史扫描** — 从 Cursor 的 state.vscdb 中扫描项目，弹窗让用户勾选导入
- **项目分组** — 创建/重命名/删除分组，拖拽项目到分组中
- **拖拽排序** — 项目卡片支持拖拽换位，带视觉插入标记

### 三种安装模式
- **全局 Skill** — 复制到 `~/.cursor/skills/`，AI 按需加载，所有项目共享
- **智能 Rule** — 复制为 `.mdc` 到项目 `.cursor/rules/`，`alwaysApply: false`
- **常驻 Rule** — 同上但 `alwaysApply: true`，每次对话都注入
- **对比表格** — 安装弹窗中展示模式/存储/加载/Token 消耗对比

### UX 审计修复（8 项）
- **#3 安装模式标签** — 已安装技能卡片显示彩色模式标签（全局Skill/智能Rule/常驻Rule/项目Skill）
- **#4 更新检测** — 安装时记录来源 URL + 内容哈希，支持一键检查和更新
- **#5 重复安装警告** — 安装前检测已存在的同名技能，确认后才覆盖
- **#6 批量操作** — 已安装区域支持批量选择模式，全选/取消/批量删除
- **#7 社区入口合并** — 「社区广场」和「我的 GitHub 社区」统一为单一「社区」导航，子标签切换
- **#8 项目路径优化** — 从 prompt 升级为模态弹窗，带 datalist 自动补全 + 历史项目点选
- **#9 使用统计** — 日志页顶部新增统计面板（调用次数、工具/技能排行、7天活跃度图表）
- **#10 导出备份** — 一键导出项目配置 JSON（含项目列表、分组、技能安装详情）

### 新手引导
- **首次使用欢迎页** — 多步骤引导介绍核心功能区域
- **Skill vs Rule 概念说明** — 安装面板内置对比解释

### 后端增强
- `SkillEntry` 新增 `installMode` 字段，索引时自动检测
- 安装注册表 `install_registry.json` 记录来源/时间/哈希
- `/api/skill/check-updates` 批量检查更新
- `/api/export-config` 导出完整配置

## v2.2.0 — 流畅度深度优化 (2026-04-09)

### 智能失效策略
- **脏标记替代数据清空** — 增删操作后不再清空所有视图数据，只标记为"脏"
- **惰性刷新** — 切换到对应视图时才检测脏位并刷新，当前视图保持不变不闪烁

### 乐观更新
- **本地删除** — 直接从内存数组 filter 掉，不再 await 重新拉取 `/api/skills`
- **上传/下架** — 只更新对应卡片的"已上传/未上传"徽章，不重建整个列表
- **GitHub 导入** — 直接 push 到本地数组并渲染，后台异步刷新缓存
- **移除阻塞调用** — 导入后不再同步 `await api('/api/community/refresh')`

### 增量 DOM 更新
- **精准节点操作** — 所有列表项添加 `data-skill` 属性，用 `CSS.escape` 精准定位
- **删除动画** — 移除单项时只操作对应 DOM 节点，带 0.2s 滑出透明动画
- **徽章局部更新** — 上传/下架只修改对应卡片的 `.upload-badge`，带缩放弹性动画

### 导航悬停预加载
- **hover 预取** — 鼠标悬停导航项时后台预取该视图数据
- **防抖保护** — 5秒内同一视图不重复预取

### 骨架屏 + 视图切换动画
- **shimmer 骨架屏** — 加载数据时显示动态渐变骨架占位，替代旋转图标和空白
- **fadeSlideIn** — 所有视图切换加 0.2s 入场动画（透明度+位移）

### 搜索防抖
- **150ms debounce** — 本地仓库、GitHub 社区、社区广场的搜索框全部加防抖
- 快速输入时只触发最后一次列表渲染，减少无效重绘

### API 请求去重
- **并发合并** — 同一 URL 的并发 fetch 请求自动合并为一个 Promise
- 快速切换视图或重复操作时避免冗余网络请求

## v2.1.0 — UI 全面优化 + 版本更新系统 (2026-04-09)

### 导航与布局
- **社区广场置顶** — 社区广场移至导航栏第一位，成为默认首页
- **分类管理内嵌** — 从独立导航项移入「我的本地仓库」模块，通过过滤栏按钮触发，在右侧面板展示
- **订阅源管理重命名** — 改为「订阅外部GitHub仓库」，语义更清晰
- **我的GitHub社区纯化** — 仅管理自己的可写仓库，不再混显外部订阅源

### 技能卡片
- **卡片化列表** — 本地仓库和GitHub社区的技能列表改为带阴影的卡片样式
- **描述预览** — 每个卡片显示1-2行描述截断，优先显示用户自定义描述
- **主题色高亮** — 卡片选中/hover时使用当前主题色渲染边框和指示条

### UI/UX 统一
- **全局圆角 24px** — 所有按钮、标签、导航项、输入框统一为 border-radius: 24px
- **主题色变量** — 新增 `--theme-active` / `--theme-active-light` / `--theme-active-text` CSS 变量，所有 active 状态颜色自动跟随主题
- **主题按钮文字化** — 从圆形 emoji 按钮改为「主题 + 当前主题名称」文字形态
- 六个主题（蜡笔小新/简洁/暗黑/海洋/森林/日落）全部适配新变量

### 版本更新系统
- **版本徽章** — 标题栏右侧显示当前版本号和 commit hash
- **自动检查更新** — 页面加载时对比本地/远程 commit，有新版本时绿色脉冲提示
- **更新详情面板** — 点击版本徽章展示更新日志、GitHub Release 信息
- **一键更新** — 执行 git pull + npm install + tsc 编译，支持重启服务
- **网络容错** — GitHub 不可达时友好提示，不阻塞页面正常使用

## v2.0.0 — 本地仓库管理重构 (2026-04-09)

### 重大变更
- **「我的本地 Skill 仓库」** — 全新双栏布局取代旧版技能浏览 + 上传/投稿分离设计
  - 左栏：搜索、状态/分类过滤、统计条、技能列表
  - 右栏：详情信息、分类管理、描述编辑、操作按钮组、子技能树/原始内容
- **侧边栏简化** — 移除分类导航树，改为 5 个清晰入口（本地仓库、社区、分类管理、MCP 管理、使用日志）
- **社区 tab 精简** — 移除「上传/投稿」tab，上传功能统一到本地仓库管理

### 新功能
- **DESCRIPTION.md 分离存储** — 上传时用户描述独立存为 DESCRIPTION.md，不再追加到 SKILL.md
- **社区卡片优先显示自定义描述** — 浏览社区技能时优先读取 DESCRIPTION.md，回退到 SKILL.md frontmatter
- **点击查看全文** — 社区技能卡片只显示描述摘要，点击名称或「查看全文」按钮加载 SKILL.md 完整内容
- **认证支持** — 订阅私有仓库获取 DESCRIPTION.md 时带 token 认证

### 移除
- `renderSkills`、`renderSkillCard`、`showSkillDetail` 本地技能旧 UI
- `renderCommunityUpload` 及相关辅助函数
- `renderCategoryNav`、`showCategory` 侧边栏分类导航

## v1.8.0 — 社区功能增强 (2026-04-08)

- 社区仓库配置、订阅外部GitHub仓库、GitHub Token 授权
- 技能上传/下架/Issue 投稿
- 链接安装、缓存机制
- MCP 管理面板
- 多主题支持（默认/简约/暗色/海洋/森林/日落）

## v1.0.0 — 初始版本

- MCP Server 基础框架
- 技能搜索、分类、详情查看
- Dashboard Web UI
